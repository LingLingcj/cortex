import { Typography } from 'antd';

const { Title } = Typography;

const Media = () => {
  return (
    <div>
      <Title level={2}>Media Library</Title>
      <p>Books, movies, and music tracking will be implemented here.</p>
    </div>
  );
};

export default Media;
