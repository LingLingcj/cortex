import { Typography } from 'antd';

const { Title } = Typography;

const Tools = () => {
  return (
    <div>
      <Title level={2}>Tools</Title>
      <p>Various tools like price tracker will be available here.</p>
    </div>
  );
};

export default Tools;
