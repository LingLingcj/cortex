import { Typography } from 'antd';

const { Title } = Typography;

const Settings = () => {
  return (
    <div>
      <Title level={2}>Settings</Title>
      <p>User settings and preferences will be managed here.</p>
    </div>
  );
};

export default Settings;
