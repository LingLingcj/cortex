import { Typography } from 'antd';

const { Title } = Typography;

const Habits = () => {
  return (
    <div>
      <Title level={2}>Habit Tracker</Title>
      <p>Habit tracking features will be implemented here.</p>
    </div>
  );
};

export default Habits;
