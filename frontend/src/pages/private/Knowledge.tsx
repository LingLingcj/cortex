import { Typography } from 'antd';

const { Title } = Typography;

const Knowledge = () => {
  return (
    <div>
      <Title level={2}>Knowledge Base</Title>
      <p>Code snippets, articles, and notes will be managed here.</p>
    </div>
  );
};

export default Knowledge;
